sudo: true
language: cpp

os: linux
stage: test
compiler: gcc

script:
    - set -e
    - mkdir build
    - cd build
    - cmake ../
    - make -j 4
    - cd ../thirdparty/ompi && make install
    - cd ../zeromq && make install
    - cd ../../build && make test
