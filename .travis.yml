sudo: true
language: cpp

os: linux
stage: test
compiler: gcc
addons: &gcc7
packages: g++-7
script:
    - set -e
    - mkdir build
    - cd build
    - cmake ../
    - make -j 2
jobs:
    - make test
